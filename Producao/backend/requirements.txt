# AYContabilidade — Backend (FastAPI + SQLAlchemy + PostgreSQL 18)
# Python 3.12

# --- API ---
fastapi>=0.118
uvicorn[standard]>=0.37
python-multipart>=0.0.20

# --- Base de dados ---
SQLAlchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2

# --- Validação e configuração ---
pydantic>=2.10
pydantic-settings>=2.7
email-validator>=2.2

# --- Autenticação ---
# bcrypt usado directamente (sem passlib): o passlib 1.7.4 está sem manutenção e
# rebenta com bcrypt >= 4.1 ao ler o atributo __about__ removido. Ver docs/LESSONS.md.
PyJWT>=2.10
bcrypt>=4.2

# --- Segundo factor (TOTP, RFC 6238) ---
# pyotp implementa o RFC e compara os códigos em tempo constante.
# segno gera o QR Code. O QR é desenhado no servidor e enviado já como imagem,
# para o segredo nunca passar pelo cliente em forma manipulável.
# Pillow compõe o PNG com a marca SGD ao centro. O SVG continua a ser o que a
# aplicação mostra — é vectorial e não pesa; o PNG existe para o QR que se
# guarda em ficheiro, porque um .svg aberto fora do browser muitas vezes não
# abre de todo.
# cryptography cifra o segredo TOTP em repouso: em claro, quem leia a base
# consegue gerar códigos de qualquer conta.
pyotp>=2.9
segno>=1.6
pillow>=11
cryptography>=44

# --- Rate limiting ---
# Atenção: nos módulos com rotas decoradas por @limiter.limit NÃO usar
# "from __future__ import annotations" (TypeError ForwardRef). Ver docs/LESSONS.md.
slowapi>=0.1.9

# --- HTTP (proxy da consulta de NIF à AGT) ---
httpx>=0.28

# --- Testes ---
# pyzbar lê o QR de volta: é o que prova que a marca ao centro não o partiu.
# Traz a libzbar consigo no Windows; onde faltar, o teste salta essa parte e
# continua a verificar o nível de correcção e o tamanho da marca.
pytest>=8.3
pytest-asyncio>=0.25
pyzbar>=0.1.9
